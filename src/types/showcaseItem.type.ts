

export interface ShowcaseItem {
  id: string;
  labelTranslationKey: string;
  descriptionTranslationKey: string;
  iconPath: string;
  imagePath: string;
  imageSource: string;
  iconSource: string;
}